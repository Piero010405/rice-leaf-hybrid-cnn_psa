defaults:
  - ../default.yml

data:
  raw_dir: data/raw/rice_leaf_disease
  split_dir: data/splits
  processed_dir: data/processed
  image_size: 224
  split:
    train: 0.70
    val: 0.15
    test: 0.15
  extensions:
    - .jpg
    - .jpeg
    - .png
    - .bmp
